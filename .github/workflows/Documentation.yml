name: Documentation
on:
  push:
    branches:
    - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Only one Pages deploy at a time. New deploys cancel in-flight ones.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    name: Build & Deploy
    runs-on: macos-26
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Need tags so we can stamp the homepage footer with the latest release.
          fetch-depth: 0

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26'

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Build DocC
        # --hosting-base-path is just the repo name; the resulting URL is
        # https://wikipediabrown.github.io/napkin/documentation/napkin/
        # --experimental-enable-custom-templates picks up header.html from
        # the napkin.docc catalog so every docs page gets the same top nav.
        run: |
          swift package --allow-writing-to-directory ./docs \
            generate-documentation \
            --target napkin \
            --disable-indexing \
            --transform-for-static-hosting \
            --hosting-base-path napkin \
            --output-path ./docs \
            --experimental-enable-custom-templates

      - name: Copy homepage
        # Hand-crafted homepage replaces DocC's missing root index.html with
        # a real landing page (nav bar, hero, feature grid, footer). The
        # nav links into /napkin/documentation/napkin/ where DocC lives.
        # __NAPKIN_VERSION__ placeholders in index.html get stamped with the
        # latest git tag at deploy time so the footer always reflects the
        # currently-shipping release.
        run: |
          cp Tools/site/styles.css docs/styles.css
          cp Tools/site/napkin-icon.png docs/napkin-icon.png
          cp Tools/site/napkin-icon@2x.png docs/napkin-icon@2x.png

          VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "main")
          sed "s/__NAPKIN_VERSION__/${VERSION}/g" Tools/site/index.html > docs/index.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
